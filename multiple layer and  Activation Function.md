# 多层神经网络(MLP)和激活函数

单层网络只有线性拟合的作用，即拟合出$y=ax+b$的形式，多层网络的表达式则为
$$
y=w1σ(w2x+b2)+b1
$$
其中σ为激活函数（Activation Function）的一种。σ(?)：ReLU / Sigmoid / Tanh 等非线性激活函数

单层神经网络和多层纯线性神经网络的效果是一样的，形式都为y=ax+b。但是多层非线性神经网络则可以拟合任意复杂的非线性函数（理论上）；折线、分段函数；高维复杂映射
$$
ReLU(x)=max(0,x)
$$

它相当于：
“当某种特征激活时，这条规则才生效”
13 个隐藏单元 ≈ 13 条“房价子规则”
每个隐藏神经元：
$$
hi?=ReLU(wiT?x+bi?)
$$
第二层做的事是：
$$
y=i∑?vi?hi?+b
$$
即把多个非线性特征组合起来
这即是为什么：线性回归 < MLP < CNN < Transformer

## 隐藏层的维度数量

这是一个**非常典型、也非常容易混淆**的问题。先给结论，再把“为什么”彻底讲清楚。

---

## 一、结论（很重要）

* 输入是 **13 维**
* 隐藏层 **可以小于 13**
* **可以等于 13**
* **也可以大于 13**

隐藏层的维度可以是任何数量

---

## 二、为什么你会产生这种疑问？（非常合理）

你现在的理解路径大概是：

> 输入是 13 维 →
> 隐藏层也是线性组合 →
> 会不会最多只能提取 13 个“方向”？

这个直觉**在“纯线性模型”里是对的**，但在神经网络里**不成立**。

---

## 三、从矩阵维度看：为什么“可以大于 13”？

### 1?? 第一层权重矩阵的形状

如果：

* 输入维度 = 13
* 隐藏层维度 = H

那么：
$$
W_1 \in \mathbb{R}^{13 \times H}
$$

? **H 可以是任意正整数**

---

### 2?? 每个隐藏神经元在干什么？

第 (i) 个隐藏神经元：

$$
h_i = \sigma(w_i^T x + b_i)
$$

其中：

* (w_i \in \mathbb{R}^{13})
* 它是输入空间中的 **一个方向 + 一个偏置**
* 激活函数引入 **非线性切分**

? 你不是在“找 13 个正交基”，而是在：

> **用很多“超平面”去切分 13 维空间**

---

## 四、一个非常关键的几何直觉（推荐你记住）

### ? 线性模型：

* 13 维输入 → **1 个超平面**

### ? 隐藏层有 H 个 ReLU：

* 13 维输入 → **H 个超平面**
* 空间被切成很多“区域”
* 每个区域内是不同的线性函数

? **H 越大，分段越细，函数越复杂**

---

## 五、为什么“隐藏层 > 13”反而很常见？

### 1?? 表达能力需求

真实世界问题：

* 特征之间高度非线性
* 存在大量条件、阈值、组合

? 常见配置：

* 输入 13
* 隐藏层 32 / 64 / 128

---

### 2?? 一个重要定理（直观版）

> **单隐藏层神经网络，在隐藏单元足够多时，可以逼近任意连续函数**（通用逼近定理）
这和输入维度没有上界关系。

---

## 六、那什么时候“隐藏层 < 13”是合理的？

### ? 情况 1：降维 / 特征压缩

```text
13 → 5 → 1
```

类似：

* PCA
* 自编码器（Encoder）

? 隐藏层变成“信息瓶颈”

---

### ? 情况 2：样本少，防止过拟合

* 波士顿房价样本只有 **506 条**
* 隐藏层太大会过拟合

? 工程上常选：

* 8
* 13
* 16

---

## 七、你现在的“13 隐藏单元”在教学中有什么意义？

在**教学示例**里：

* 输入 13
* 隐藏 13

是为了：

1. 维度好理解
2. 不引入额外复杂度
3. 梯度推导清晰

? **不是理论限制，而是教学选择**

---

## 八、一个常见误区（必须指出）

? 错误理解：

> 隐藏层神经元 = 输入特征数

? 正确理解：

> 隐藏层神经元 = **可学习的非线性特征个数**

---

## 九、用一句话帮你“定性理解”

> **输入维度决定的是“每个神经元看多少信息”，
> 隐藏层维度决定的是“模型有多少种非线性切分方式”。**
